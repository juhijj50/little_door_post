# Leave empty in development — Vite proxies /api to http://localhost:8000.
# Set this to the deployed API's origin (no trailing slash) for production:
# VITE_API_URL=https://post-api.example.com
VITE_API_URL=
